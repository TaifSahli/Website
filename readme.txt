
Command	Description
npm install --silent	
This would install all the required dependencies in the node_modules folder.
npm run build
Compile sass files and generate css files .
npm start	
Runs the project locally, starts the development server. The development server is accessible at http://localhost:5500.
